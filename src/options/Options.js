import './Options.css';
import Popup from '../Popup';

function Options() {
  return (
    <div className="App">
      <header className="App-header">
        <Popup/>
      </header>
    </div>
  );
}

export default Options;
